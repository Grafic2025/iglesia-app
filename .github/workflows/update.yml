name: Actualizar App Automática
on:
  push:
    branches:
      - main

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repo
        uses: actions/checkout@v3

      - name: ⚙️ Configurar Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 🚀 Instalar EAS CLI y dependencias
        run: |
          npm install --global eas-cli
          npm install

      - name: 📱 Publicar EAS Update
        run: eas update --auto --branch main
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
