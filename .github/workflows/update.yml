name: Actualizar App Automática
on:
  push:
    branches:
      - main

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repo
        uses: actions/checkout@v3

      - name: ⚙️ Configurar Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: 🚀 Configurar Expo y EAS
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-cache: true
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 📱 Publicar EAS Update
        run: npx eas-cli update --auto --branch main
